'use client';
import { Tabs,TabsList,TabsTrigger } from '@/components/ui/tabs';
import { Trophy } from 'lucide-react';
import { useEffect,useState } from 'react';
import { api,avatar,Loading,type Props } from './shared';
export function Leaderboard({t,locale}:Props){const [period,setPeriod]=useState('week'),[rows,setRows]=useState<any[]|null>(null),[error,setError]=useState('');useEffect(()=>{let active=true;setRows(null);api('leaderboard',undefined,{period}).then(d=>{if(active){setRows(d.rows);setError('')}}).catch(e=>active&&setError(e.message));return()=>{active=false}},[period]);return <div className="stack"><div className="section-head"><div><p className="eyebrow">{t('A LITTLE FRIENDLY COMPETITION','تحدٍ بروح ودية')}</p><h1>{t('The wisdom board.','لوحة الحكمة.')}</h1></div><Trophy size={36}/></div><p className="muted">{t('Every correct answer counts. Missing Letters earns 15 points, Multiple Choice 10 and Cryptogram 20.','كل إجابة صحيحة تُحتسب. الحروف الناقصة: ١٥ نقطة، الاختيار المتعدد: ١٠، الشفرة: ٢٠.')}</p><Tabs value={period} onValueChange={setPeriod}><TabsList className="segmented"><TabsTrigger value="week">{t('Last 7 days','آخر ٧ أيام')}</TabsTrigger><TabsTrigger value="all">{t('All time','كل الأوقات')}</TabsTrigger></TabsList></Tabs>{error?<div className="notice error">{error}</div>:!rows?<Loading/>:rows.length===0?<div className="panel empty"><Trophy size={32} style={{margin:'auto'}}/><h2 style={{marginTop:15}}>{t('The board is waiting.','اللوحة بانتظارك.')}</h2><p>{t('Solve a puzzle while signed in to earn your place.','حل لغزًا بعد تسجيل الدخول لتظهر هنا.')}</p><a href="/" className="btn">{t('Choose a game','اختر لعبة')}</a></div>:<div className="panel table-scroll"><table className="data-table"><thead><tr><th>#</th><th>{t('Player','اللاعب')}</th><th>{t('Puzzles solved','الألغاز المحلولة')}</th><th>{t('Points','النقاط')}</th></tr></thead><tbody>{rows.map((p,i)=><tr key={p.id}><td>{i===0?<Trophy size={19}/>:i+1}</td><td><div className="flex-row">{avatar(p)}{p.name}</div></td><td>{p.solved}</td><td><strong>{p.points}</strong></td></tr>)}</tbody></table></div>}</div>}